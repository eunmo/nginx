### Link config file: `cd /etc/nginx && sudo ln -s <root_dir>/nginx.conf`

### Restart nginx: `sudo nginx -s reload`

### Restart servers: `./forever.sh`

### Update ssl settings: `sudo certbot --nginx`
